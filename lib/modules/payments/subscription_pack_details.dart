import 'package:flutter/material.dart';

class SubscriptionPackDetailScreen extends StatelessWidget {
  const SubscriptionPackDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
