import 'package:flutter/material.dart';

class SubscriptionPackScreen extends StatelessWidget {
  const SubscriptionPackScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
