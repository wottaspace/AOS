import 'package:flutter/material.dart';

class SuccessFundedJobScreen extends StatelessWidget {
  const SuccessFundedJobScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
