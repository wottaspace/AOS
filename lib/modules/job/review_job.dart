import 'package:flutter/material.dart';

class ReviewJobScreen extends StatelessWidget {
  const ReviewJobScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
