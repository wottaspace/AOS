import 'package:flutter/material.dart';

class JobDetailsModalScreen extends StatelessWidget {
  const JobDetailsModalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
