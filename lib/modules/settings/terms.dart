import 'package:flutter/material.dart';

class ContractTermsScreen extends StatelessWidget {
  const ContractTermsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
