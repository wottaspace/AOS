import 'package:flutter/material.dart';

class LoremIpsumScreen extends StatelessWidget {
  const LoremIpsumScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
